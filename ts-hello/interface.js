var drawPoint = function (x, y) {
    //....operations
};
// we can group thos parameters together
var drawPointWithPoint = function (point) {
    //..... draw a point
};
var drawAnotherPoint = function (point) {
    // draw points
};
